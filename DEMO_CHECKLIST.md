# GateTrace 데모 체크리스트

## 증거 게이트

다음 항목을 확보하기 전에는 발표에서 통합 완료나 결과로 표현하지 않습니다.

- [x] 결정론·대역 기반 로컬 테스트 전체 통과. 외부 서비스 통합 증거로는 사용하지 않음
- [x] 정상 샘플 입력이 실제 GateTrace 실행으로 전달되어 `APPROVED` 반환
- [x] 오염 샘플 입력이 실제 GateTrace 실행으로 전달되어 `QUARANTINED` 반환
- [x] Nosana `qwen/qwen3.8-27b`가 네 게이트 명세와 한영 요약을 생성, 생성 플래그 확인
- [x] Daytona가 GateTrace 검증 코드를 실행한 샌드박스 ID와 실행 시간 확인, 삭제 후 목록 미존재 확인
- [x] `APPROVED` 판정, 구조화 근거, 결정론적 한영 fallback 설명이 함께 보이는 결과
- [x] `QUARANTINED` 판정, 구조화 근거, 결정론적 한영 fallback 설명이 함께 보이는 결과
- [x] 공개 저장소: https://github.com/geondongkim/gatetrace-daytona
- [x] Vercel 프로덕션 배포 https://gatetrace-daytona.vercel.app 의 루트·헬스·정상 샘플 E2E 확인
- [x] 배포 URL 320×812에서 가로 오버플로우 없음, 모바일 메뉴 동작 확인
- [ ] 캡처와 로그에 API 키, 토큰, 이메일, 내부 URL, 원본 민감 데이터가 없는지 확인

## 라이브 데모 순서

목표 시간은 약 40초입니다. 실제 런타임 증거를 확보한 뒤에만 이 순서를 사용합니다.

1. GateTrace가 논문·연구에 사용할 데이터를 검증하는 제품임을 먼저 설명하고, 이번 실증 예시인 정상/오염 센서 샘플을 가리킵니다.
2. 검증 실행을 시작하고 요청 식별자를 기록합니다.
3. Nosana가 만든 제한적 검증 명세에서 허용된 네 검사 항목을 한 줄로 보여 줍니다.
4. Daytona 샌드박스 ID와 실행 시간을 보여 줍니다.
5. 결과 화면에서 `APPROVED` 또는 `QUARANTINED`, 구조화 근거, 한영 설명을 차례로 짚습니다.
6. 반대 유형 샘플의 사전 실행 결과를 열어 두 판정이 데이터 근거에 따라 달라지는지 보여 줍니다.

## 스크린샷 대체 순서

라이브 실행이 늦어지거나 네트워크가 불안정하면 10초 안에 대체 순서로 전환합니다.

1. `output/playwright/gatetrace-bilingual-clean.png`: 입력부터 정상 결과까지 전체 화면
2. `output/playwright/gatetrace-result-clean.png`: `APPROVED`와 네 게이트의 구조화 근거
3. `output/playwright/gatetrace-result-contaminated.png`: `QUARANTINED`와 세 실패 게이트의 구조화 근거

대체 설명은 다음 순서로 진행합니다.

1. 입력 화면에서 같은 연구 목표에 정상/오염 샘플을 선택했다고 설명합니다.
2. 검증 명세 화면에서 시스템이 실행 범위를 제한한다고 설명합니다.
3. 결과 화면의 Daytona 샌드박스 ID와 실행 시간을 근거로 격리 실행을 설명합니다.
4. 두 결과 화면에서 데이터 근거에 따라 판정이 달라졌음을 설명합니다. Nosana 모델 ID가 비어 있으면 반드시 fallback 설명이라고 밝힙니다.

## 발표 직전 점검

- [ ] `presentation/GateTrace_3min_Pitch.marp.md`가 정확히 8장인지 확인
- [x] 모든 장표에 약 15초의 발표자 노트가 있고 8장 전체가 약 2분인지 확인
- [ ] `[확인 필요]`를 실제 증거로 교체했거나, 남은 표기를 발표에서 계획으로 명확히 설명
- [ ] 로컬 상대 경로로 테마와 글꼴이 로드되는지 확인
- [ ] PDF와 HTML에서 글자 잘림, 겹침, 깨진 한글이 없는지 확인
- [x] 앱의 `/styles.css`와 `/app.js` 200 응답 및 첫 화면 렌더링 확인
- [x] 데모 탭과 정상·오염 결과 캡처를 준비
- [ ] 비밀값과 개인 정보가 화면, 셸 기록, 로그, URL에 없는지 재확인
- [x] 네트워크 실패 시 사용할 정상·오염 결과 캡처를 로컬에 보관
- [ ] 발표 자료의 공개 저장소·데모 링크를 확인하거나 `[확인 필요]` 상태로 유지
- [x] `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -p no:cacheprovider -q` 18개 통과, 외부 서비스 대역 테스트임을 구분

## 데모 후 기록

- [ ] 실행 시각, 입력 샘플 해시 또는 공개 가능한 식별자, 요청 식별자를 기록 **[확인 필요]**
- [ ] Nosana 응답과 Daytona 실행 로그에서 비밀값을 제거한 재현 자료를 보관 **[확인 필요]**
- [ ] 판정과 기대값의 일치 여부를 기록하고 불일치 시 원인을 분리 **[확인 필요]**
- [ ] 발표 중 사용한 정확한 커밋 또는 아카이브 식별자를 기록 **[확인 필요]**
